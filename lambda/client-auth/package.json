{
  "name": "pitfal-client-auth-lambda",
  "version": "1.0.0",
  "description": "Client gallery authentication for Pitfal Solutions",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/*"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/bcryptjs": "^2.4.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
